<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.lgw.android.testmodule">

    <application
        android:allowBackup="true"
        android:debuggable="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.LearningDemo"
        tools:ignore="HardcodedDebugMode">
        <service
            android:name=".service.PushService"
            android:enabled="true"
            android:exported="true"
            android:process=":push" />

        <activity android:name=".MessengerActivity" />

        <service
            android:name=".messenger.MessengerService"
            android:enabled="true"
            android:exported="false"
            android:process=":remote">
            <intent-filter>
                <action android:name="com.lgw.android.testmodule.messenger.MessengerService" />
            </intent-filter>
        </service>

        <activity android:name=".ThirdActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".SecondActivity"
            android:process=":lgw.2" />
        <activity android:name=".MainActivity">

        </activity>
    </application>

</manifest>